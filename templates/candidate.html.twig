{% extends "base.html.twig" %}

{% block body %}
    <div class="container">
        <div class="jumbotron">
            <h1 class="display-4">{{ 'home_page.title'|trans }}</h1>
            {{ form_start(form)}}
            <div class="row mt-4">
                <div class="col-md-5">
                    {{ form_row(form.firstName) }}
                </div>
                <div class="col-md-5 offset-1">
                    {{ form_row(form.lastName) }}
                </div>
            </div>
            <button class="btn btn-primary">{{ 'front.candidate.start' | trans}}</button>
            {{form_end(form)}}
        </div>
    </div>
    <script>
    </script>
{% endblock %}
